@model IEnumerable<KnockoutMVC.Models.MTB_Articles>

@{
    ViewBag.Title = "Index";
}

<!--<h2>Index</h2>

<p>
 <a class="btn btn-primary" href="@Url.Action("Create","MTB_Articles")">Create New</a>   
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Excerpts)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Excerpts)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>-->
 
<h2>Articles</h2>
<p>
    <a href="@Url.Action("Create", "MTB_Articles")" class="btn btn-primary btn-large">Create New »</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Excerpts</th>
            <th>Content</th>
        </tr>
    </thead>
    <tbody data-bind="foreach: Articles">
        <tr>
            <td data-bind="text: Title"></td>
            <td data-bind="text: Excerpts"></td>
            <td data-bind="text: Content"></td>
        </tr>
    </tbody>
</table>
@section Scripts {
    @*@Scripts.Render("~/Scripts/ViewModel/IndexVM.js")*@
    <script src="~/Scripts/app/IndexVM.js"></script>
}